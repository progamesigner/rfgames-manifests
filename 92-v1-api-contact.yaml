---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: v1-api-contact
  namespace: default
  labels:
    app.kubernetes.io/name: v1-api-contact
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: v1-api-contact
  template:
    metadata:
      labels:
        app.kubernetes.io/name: v1-api-contact
    spec:
      containers:
      - name: rfgames-contact-api-server
        image: progamesigner/rfgames-contact-api-server:1.2
        env:
        - name: CONTACT_WEBHOOK_URL
          valueFrom:
            configMapKeyRef:
              key: CONTACT_DISCORD_WEBHOOK
              name: api-server-configs
        ports:
        - name: http
          containerPort: 80
          protocol: TCP
        livenessProbe:
          tcpSocket:
            port: http
        readinessProbe:
          tcpSocket:
            port: http
        resources:
          limits:
            cpu: 200m
            memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: v1-api-contact
  labels:
    app.kubernetes.io/name: v1-api-contact
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: v1-api-contact
